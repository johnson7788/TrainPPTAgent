# 谷歌Gemini模型，下面使用哪个模型，就配置哪个模型的key
GOOGLE_API_KEY=xxx
DEEPSEEK_API_KEY=xxx
ALI_API_KEY=sk-xxx
OPENAI_API_KEY=xxx
CLAUDE_API_KEY=xxx

# 一般不用修改
OUTLINE_API=http://127.0.0.1:10001
CONTENT_API=http://127.0.0.1:10011
#知识库的访问链接
PERSONAL_DB=http://127.0.0.1:9100

# 是否使用代理，clash的代理7890， 如果使用Openai等模型，可以开启，如果本地有代理
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# 使用哪个模型供应商, ollama, 或者vllm，需要提供OLLAMA_API_URL， VLLM_API_URL
# 如果使用本地模型，请阅读custom_model.md
# 生成大纲使用的模型
MODEL_PROVIDER=deepseek
LLM_MODEL=deepseek-chat

#不同的Agent使用不同的模型，一个是PPT撰写者，1个是PPT检查者，更多的模型请参考backend/slide_agent/slide_agent/create_model.py
PPT_WRITER_PROVIDER=ali
PPT_WRITER_MODEL=qwen-turbo-latest

PPT_CHECKER_PROVIDER=ali
PPT_CHECKER_MODEL=qwen-turbo-latest

# 是否使用绘图，如果模型比较小，可以关掉绘图，因为绘图时对模型收集数据和生成固定格式的JSON要求较高
USE_CHART=True

# 向量库的配置
# 向量模型API的Key, 在文件embedding_utils.py中被使用， 阿里云，
# 支持embedding厂商：aliyun，doubao，vllm，xinference，ollama

#ALI_API_KEY=sk-xxx
#EMBEDDING_PROVIDER=aliyun
#EMBEDDING_MODEL=text-embedding-v4

# 火山引擎，豆包
EMBEDDING_PROVIDER=doubao
EMBEDDING_MODEL=doubao-embedding-text-240715
DOUBAO_API_KEY=xxx
# 如果有GPU，那么可以开启这个，否则CPU太慢,如果需要USE_MINERU为True，那么mineru[core]>=2.0.6
# Mineru自动下载模型
USE_MINERU=false

